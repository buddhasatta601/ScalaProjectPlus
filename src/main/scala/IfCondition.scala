object IfCondition {
  def main(args :Array[String]): Unit = {
    var age =19;
    var min_age_limit=18;
    if (age>min_age_limit){
      println("He is Allowed in the Vote");
    }
    else{
      println("He is not allowed to vote");
    }
  }

}
